VITE_API_URL=https://your-backend-domain.up.railway.app
# Sentry DSN (public — safe to include in frontend bundle)
# Get it from: sentry.io → project → Settings → Client Keys (DSN)
VITE_SENTRY_DSN=
# Optional: set in Railway to tag releases in Sentry
VITE_APP_VERSION=
